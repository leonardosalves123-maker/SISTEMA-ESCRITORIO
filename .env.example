# Banco de dados (SQLite local). Para produção, troque por uma URL PostgreSQL.
DATABASE_URL="file:./dev.db"

# Segredo usado para assinar as sessões de login (JWT).
# Em produção, use uma string longa e aleatória.
SESSION_SECRET="troque-este-valor-em-producao"
